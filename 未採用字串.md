# 未採用的字串與原因

你回報的 330 條裡，有一部分**刻意沒有**加進字典。不是漏翻，是加進去會把你的資料改掉或產生錯誤資訊。

| 字串 | 不採用的原因 |
|---|---|
| `(→ github.com)` | 網域片段，不可翻譯 |
| `, including the automatic setup, won’t count towards your usage lim…` | 開頭逗號，DOM 切斷的殘缺句 |
| `. I’ll keep an eye on what’s running and post updates when somethin…` | 開頭句點，DOM 切斷的殘缺段落 |
| `/ GitHub` | 麵包屑殘缺片段 |
| `12:58 PM` | 時間，不可翻譯 |
| `1788621976697_image.png, 1788621849947_image.png` | 檔名，不可翻譯 |
| `21.3 kB` | 檔案大小，不可翻譯 |
| `25 FREE` | 數字代號，語意不明 |
| `258_flyer.html` | 檔名，不可翻譯 |
| `3 post ideas` | 使用者自訂內容標題 |
| `36.5k` | 數字，不可翻譯 |
| `3m` | 數字代號，不可翻譯 |
| `401k` | 數字，不可翻譯 |
| `8:25 AM` | 時間，不可翻譯 |
| `A few things I could pick up now:` | Claude 動態產生的訊息內容 |
| `Actions for claude-zh-tw.user.js` | ~~含檔名的動態標籤~~ v3.3.0 改用規則 `^Actions for (.+)$`（僅屬性），檔名照原樣帶入 |
| `Add a` | DOM 切斷的殘缺片段 |
| `Add that Priya owns the usage charts` | 使用者記憶內容，含人名 |
| `Added b010203044-code/zh-tw-webui to this thread` | ~~含帳號儲存庫名的動態訊息~~ v3.3.0 改用規則 `^Added (.+) to this thread$`，儲存庫名照原樣帶入 |
| `Adding repository` | Claude 即時動態狀態文字 |
| `Alt` | 鍵盤按鍵名，語意不明 |
| `Apr 12` | 日期，不可翻譯 |
| `April` | 日期，不可翻譯 |
| `Artifact` | ~~術語保留英文，無需條目~~ v3.12.0 改譯為「作品」，`Artifact` 與 `Artifacts` 都已進字典 |
| `Assemble the multi-site v3 script and syntax-check` | Claude 動態工作狀態文字 |
| `CDN` | 術語保留英文，無需條目 |
| `Checking connectors` | 動態即時狀態文字 |
| `Chromium` | 產品名稱 |
| `Claude` | 產品名稱，保留英文 |
| `Claude Code` | 產品名稱，保留英文 |
| `Claude GitHub` | 產品名稱，保留英文 |
| `Claude reacted with eyes` | 含變動名稱與表情的通知 |
| `Cloudflare Developer Platform` | 連接器與產品名稱 |
| `Connectors: Notion` | ~~含連接器名稱的動態標籤~~ v3.3.0 改用規則 `^Connectors: (.+)$`，連接器名照原樣帶入 |
| `Ctrl` | 按鍵名稱，保留英文 |
| `Daily at 9:00 AM` | ~~含時間的動態排程文字~~ v3.3.0 改用規則 `^Daily at (.+)$`，時間照原樣帶入 |
| `Drive` | 連接器名稱，保留英文 |
| `Fable 5.1` | 模型名稱，保留英文 |
| `Firecrawl` | 連接器名稱，保留英文 |
| `GitHub` | 品牌名稱，保留英文 |
| `GitHub App` | 產品名稱，保留英文 |
| `Gmail` | 連接器名稱，保留英文 |
| `Google Drive` | 連接器名稱，保留英文 |
| `Haiku 4.5` | 模型名稱，保留英文 |
| `Harry` | 人名 |
| `Hold p95 API latency under 200 ms` | 使用者輸入的記憶內容 |
| `MEMORY.md` | 檔名，不可翻譯 |
| `Max` | 方案名稱，保留原文 |
| `Mem` | 連接器名稱 |
| `Monday, September 21, 2026` | 日期，動態內容 |
| `Note that releases go out on Tuesdays` | 使用者記憶內容 |
| `Notion` | 連接器名稱，保留原文 |
| `Opus 5` | 模型名稱，不翻譯 |
| `PR` | 慣例保留英文，不入字典 |
| `Pasted text.txt` | 檔名，使用者資料 |
| `Plan usage, Compacts automatically` | DOM 拼接片段，非單一字串 |
| `Price card page verification` | 討論串標題，動態內容 |
| `Pro` | 方案名稱，保留原文 |
| `RAG` | 慣例保留英文，不入字典 |
| `README.md` | 檔名，不可翻譯 |
| `Raw` | 慣例保留英文不翻 |
| `Shift` | 鍵盤按鍵名稱不翻 |
| `Sonnet 5` | 模型名稱保留英文 |
| `Tampermonkey` | 產品名稱保留英文 |
| `Think step by step and show reasoning for complex problems. Use spe…` | 使用者偏好設定的範例文字 |
| `Threads do the work on their own, and you can see their statuses in` | 被 DOM 切斷的殘缺句子 |
| `TickTick` | 連接器名稱，保留英文 |
| `Todoist` | 連接器名稱，保留英文 |
| `Trello` | 連接器名稱，保留英文 |
| `Ultracode` | 產品／專有名稱 |
| `UserScript` | 術語規定保留英文 |
| `Welcome, Harry.` | 含使用者人名 |
| `Wix` | 連接器名稱，保留英文 |
| `WordPress.com` | 網域／服務名稱 |
| `Working on things! I’ll ping you.` | Claude 即時回覆內容 |
| `done` | 小寫通用字，疑為狀態值 |
| `in progress` | 小寫通用字，疑為狀態值 |
| `index.html` | 檔名 |
| `main` | 分支名稱 |
| `not done` | 小寫通用字，疑為狀態值 |
| `now` | 相對時間戳記 |
| `repo` | 網址佔位／代碼片段 |
| `owner` | 網址佔位／代碼片段 |
| `running` | 小寫狀態值，易誤替換 |
| `status` | 代碼識別字 |
| `Script Installation` | Tampermonkey 擴充功能頁，@match 不涵蓋 |
| `This page is used as an intermediate step to install a new userscri…` | Tampermonkey 擴充功能頁，@match 不涵蓋 |
| `It should automatically be closed or redirected after the installat…` | Tampermonkey 擴充功能頁，@match 不涵蓋 |
| `The page is not intended to be viewed directly. Please visit the do…` | Tampermonkey 擴充功能頁，@match 不涵蓋 |
| `Firecrawl icon` | 含變動內容，已改用數字／組合規則 |
| `Filter by type: All types` | 含變動內容，已改用數字／組合規則 |
| `Default (Opus 5)` | 含變動內容，已改用數字／組合規則 |
| `Up to $25 of initial usage` | 含變動內容，已改用數字／組合規則 |
| `Valid for 24 hours after you create the project, or until the $25 i…` | 含變動內容，已改用數字／組合規則 |
| `Help me set up my code connection` | 建議提示，點了會變成你的訊息 |
| `Set up this project from my recent work` | 建議提示，點了會變成你的訊息 |
| `By Anthropic. A deck you can present, restyle with a design system …` | Artifact 型別目錄資料，作者與描述可變 |
| `By Anthropic. A document your team reads, comments on and edits in …` | Artifact 型別目錄資料，作者與描述可變 |
| `By Anthropic. Screens, flows and graphics laid out as artboards on …` | Artifact 型別目錄資料，作者與描述可變 |
| `cloud environment` | DOM 切斷的殘缺片段 |
| `List end` | DOM 切斷的殘缺片段 |

---

## 幾個值得單獨講的

- **Claude 做事時的即時狀態文字**（`Assemble the multi-site v3 script and syntax-check`、`Checking connectors`、`Fetching https://…`）每次都不一樣，寫死進字典只會讓字典無限膨脹卻永遠翻不到。
- **被 DOM 切斷的片段**（`Add a`、`The Claude`、`https://gith`）是蒐集時跨越元素邊界造成的，翻了會出現半截中文。
- **含金額或時數的促銷文字**（`Up to $25 of initial usage`）一旦官方改數字，翻譯就變成錯誤資訊，比漏翻更糟。已改成規則，金額照原樣帶入。
- **建議提示按鈕**（`Help me set up my code connection`）點下去是把那段文字當成你的訊息送出，等同使用者內容。
- **Tampermonkey 自己的安裝頁**在 `chrome-extension://` 來源，腳本的 `@match` 只有 claude.ai 與 github.com，永遠不會執行。要翻得另外加 `@match` 並另建站台字典。


---

# 第二批（2026-09-21，v3.3.0）

你這次回報 511 條。其中 **213 條早就在字典裡**，另有約 60 條本來就由數字規則涵蓋
（`N replies`、`N% of 5-hour limit`、`N of 50,000 files`、`Open thread — N replies` 這一類），
**新收 15 條字典 + 6 條規則**。剩下的沒收，理由分類如下。

| 類別 | 例子 | 不採用的原因 |
|---|---|---|
| 你貼的清單本身 | `'\'(edited)\': \'\','` 開頭那半截 | 那是你上一份清單在網頁上被當成程式碼區塊顯示，再被蒐集工具抓下來的文字，不是介面字串 |
| 時間與日期 | `10:35 AM`、`Jul 11`、`Monday, September 21, 2026` | 每分鐘都在變，翻了只會是錯的 |
| 檔名 | `1789955902197_image.png`、`image.png`、`index.html` | 你的資料 |
| 純數字與代號 | `363.5k`、`3m`、`21.3 kB` | 沒有可翻譯的字 |
| Claude 做事時的即時狀態 | `Applying review corrections and building dictionary additions`、`Searching past chats`、`Running workflow`、`Using artifact tool`、`330 strings categorized + 33 cross-check issues found…` | 每次都不一樣，寫死進字典只會讓字典無限膨脹卻永遠翻不到 |
| 討論串名與範例內容 | `Billing migration`、`Finalize the Q3 launch plan`、`Track weekly operational numbers`、`Research what customers say about our product`、`first item` | 你的資料，或官方示範用的假資料 |
| 建議提示按鈕 | `Add this to your project for shared context` | 點下去是把那段文字當成你的訊息送出 |
| 載入動畫的趣味動詞 | `Forming`、`Kneading`、`Molding`、`Turning over` | 官方會不定期換一批，而且這幾個字太短，翻了容易誤中你的內容 |
| 模型與方案名稱 | `Opus 4.6`、`Sonnet 4.6`、`Fable 5`、`Haiku 4.5` | 慣例保留英文 |
| 連接器與產品名稱 | `Firecrawl`、`Gmail`、`Google Drive`、`Wix`、`WordPress.com`、`Chromium` | 品牌名，保留原文 |
| 術語縮寫與檔案類型標籤 | `DOM`、`JS`、`TXT`、`CDN`、`PR`、`RAG` | 慣例保留英文 |
| 鍵盤按鍵名 | `Alt`、`Ctrl`、`Shift` | 台灣習慣看英文鍵名 |
| 網域 | `claude.ai`、`github.com`、`stackoverflow.com` | 不可翻譯 |
| 含人名或帳號 | `Welcome, Harry.`、`Install & Authorize on your personal account b010203044-code` | 你的資料。（`Good morning, Harry` 例外，已改成規則保留名字） |
| 兩段黏在一起 | `All repositoriesThis applies to all current and future repositories…`、`Only select repositoriesSelect at least one repository…`、`Usage: 0% of 5-hour limit, Compacts automatically` | 蒐集時跨越元素邊界，網頁上其實是分開的兩段，已分別收錄 |
| 被 DOM 切斷的片段 | `Add a`、`Ask Claude a`、`for these repositories:`、`with these permissions:`、`code, issues,`、`and write`、`access to`、`the`、`your`、`up`、`do` | 翻了會出現半截中文 |
| Tampermonkey 自己的頁面 | `The page is not intended to be viewed directly…` | 在 `chrome-extension://` 來源，腳本的 `@match` 永遠不會執行 |

## 這次新收的（供你對照）

**字典 15 條**：`(optional)`、`List`、`Project`、`Select`、`Thread`、`Add a Google Drive folder`、
`Drop in files or folders, or add manually`、`Close image preview`、`Uploading image`、
`How do usage and length limits work?`、`Usage limit best practices`、`What are projects?`、
以及 GitHub App 授權頁的 3 條權限說明。

其中 `List`、`Project`、`Select`、`Thread` 是泛用單字，放進 `attrOnly`：
只有 `aria-label` / `title` 會翻，畫面上剛好叫這幾個字的檔案、資料夾或討論串不會被動到。

**規則 6 條**：`Actions for X`（僅屬性）、`Connectors: X`、`Added X to this thread`、
`Daily at X`、`Sort by: X`、`Good morning/afternoon/evening, X`。
變動的部分（檔名、連接器名、儲存庫名、時間、人名）一律照原樣帶入，不翻。

---

# 第三批（2026-09-21，v3.5.0，用 `Ctrl + Alt + D` 盤點）

診斷回報 48 條，**新收 25 條字典 + 9 條規則**，剩下 14 條不採用：

| 字串 | 不採用的原因 |
|---|---|
| `b010203044-code/zh-tw-webui` | 儲存庫名稱，你的資料 |
| `zh-tw-webui.user`、`zh-tw-webui.user.js`、`js` | 檔名與副檔名 |
| `Price card page verification`、`Marketing analytics` | 討論串名稱，你的資料 |
| `v3.3.0 commit` | 附件連結的標題，動態內容 |
| `https://github.com/...`、`https://raw.githubusercontent.com/...` | 網址 |
| `github.com` | 網域 |
| `Harry` | 人名 |
| `Claude` | 產品名稱，保留英文 |
| `Opus 5` | 模型名稱，保留英文 |
| `Pro` | 方案名稱，保留原文 |
| `Ctrl`、`Shift` | 鍵盤按鍵名，台灣習慣看英文 |
| `Monday, September 21, 2026` | 日期 |

`Download zh-tw-webui.user.js` 和 `More options for Price card page verification` 沒有直接入庫，
改成規則 `^Download (.+)$` 與 `^More options for (.+)$`，後面的檔名與討論串名照原樣帶入。

`High`、`Low`、`Mode`、`Move`、`Prompt` 收進字典但同時列入 `attrOnly`：
只有 `aria-label` 會翻，畫面上剛好叫這幾個字的檔案或討論串不會被動到。


# 第四批（2026-09-21，v3.6.0，第二次 `Ctrl + Alt + D` 盤點）

回報的 44 條是在 **v3.4.0** 上盤點出來的（清單標頭寫「字典：529 條　規則：39 條」），
其中 27 條 v3.5.0 早就翻好了。真正還沒涵蓋的只有 17 條，**新收 5 條字典**，剩下 12 條不採用：

| 字串 | 不採用的原因 |
|---|---|
| `b010203044-code/zh-tw-webui` | 儲存庫名稱，你的資料 |
| `Price card page verification`、`Marketing analytics` | 討論串名稱，你的資料 |
| `Harry` | 人名 |
| `Claude` | 產品名稱，保留英文 |
| `Opus 5` | 模型名稱，保留英文 |
| `Pro` | 方案名稱，保留原文 |
| `Intercom` | 第三方產品名稱 |
| `Ctrl`、`Shift` | 鍵盤按鍵名，台灣習慣看英文 |
| `Monday, September 21, 2026` | 日期 |
| `done` | 全小寫的狀態值，不是畫面上的介面文字 |

新收的 5 條：`Message actions`、`Claude finished the response`、`Reply`、`Added`、`to this thread`。

**`Added` 與 `to this thread`要特別說明。** 這兩條是同一句
「Added b010203044-code/zh-tw-webui to this thread」被 DOM 切成三個文字節點的兩端，
中間的儲存庫名是獨立元素。v3.3.0 為這句加的規則 `^Added (.+) to this thread$`
要求整句在同一個文字節點裡，所以在真實頁面上**從來沒有命中過**。
改成翻兩端的碎片後會組成「已新增 <儲存庫名> 至此討論串」，規則本身保留並把用詞改成一致。
這是「DOM 切斷的殘缺片段一律不收」這條規矩的例外：兩端都收、而且翻完語順正確時才做。


# 第五批（2026-09-21，v3.7.0，專案與討論串列表頁）

回報 36 條。其中 `Message actions`、`Reply` v3.6.0 已收，**新收 12 條字典**，剩下 22 條不採用：

| 字串 | 不採用的原因 |
|---|---|
| `README.md`、`zh-tw-webui.user.js`、`zh-tw-webui.user`、`js`、`JS`、`MD` | 檔名與副檔名標籤 |
| `b010203044-code/zh-tw-webui` | 儲存庫名稱，你的資料 |
| `github.com` | 網域 |
| `https://github.com/.../commit/18b521f v3.3.0 commit`、`v3.3.0 commit` | 附件連結的網址與標題 |
| `v3.5.0 localization: 48 strings categorized, 25 dict + 9 rules, tests pass` | 討論串標題，你的資料 |
| `zh-tw-webui localization workflow documented; version auto-bump by change size` | 討論串標題，你的資料 |
| `Marketing analytics`、`Price card page verification` | 討論串名稱，你的資料 |
| `Harry` | 人名 |
| `Claude` | 產品名稱，保留英文 |
| `Opus 5` | 模型名稱，保留英文 |
| `Pro` | 方案名稱，保留原文 |
| `Ctrl`、`Shift` | 鍵盤按鍵名，台灣習慣看英文 |
| `Monday, September 21, 2026`、`Sep 21, 2026, 12:08 PM` | 日期與時間 |

新收的 12 條分兩類。**六條照常翻**（多字詞，不可能跟你的命名撞在一起）：
`Sort by`、`Group by`、`Custom groups`、`Date created`、`Last activity`、`Routines`。

**六條收進字典但同時列入 `attrOnly`**：`All`、`Active`、`Idle`、`Name`、`Type`、`Status`。
這六個是泛用單字，只有 `aria-label` / `title` 會翻，**畫面上仍然是英文**。
理由跟 v3.2.0 那次一樣：你的討論串或專案剛好叫 `Status`、`Name` 的話，
整段完全相同的比對會把它一起改掉。要讓它們在畫面上也變中文就得放棄這層保護，
這是取捨，不是漏翻。

---

## v3.10.0：改成自動過濾，不再列進盤點清單

上面整理的類別有一部分已經寫進腳本，盤點與背景累積時自動排除，不會再出現在你按
`Ctrl + Alt + E` 拿到的清單裡。翻譯行為完全沒變，只是少了固定雜訊。

**`looksLikeData()` 自動濾掉的**：

| 類別 | 例子 |
|---|---|
| 含諺文或假名 | `주소: 500 Howard Street…`（Anthropic 韓國法人頁尾） |
| 網址 | `https://github.com/…/commit/5afca80` |
| email | `support@anthropic.com` |
| 檔名、網域、儲存庫路徑（整串無空白又帶 `.` 或 `/`） | `README.md`、`github.com`、`b010203044-code/zh-tw-webui` |
| 30 字以內、含星期或月份縮寫的日期 | `Monday, September 21, 2026`、`Sun, Sep 27, 5:00 PM` |

**站台設定的 `never` 清單擋掉的**（產品名、方案名、按鍵名，本來就保留英文）：

`Anthropic`、`Anthropic PBC`、`PBC`、`Claude`、`Claude Code`、`Opus`、`Sonnet`、`Haiku`、
`Opus 5`、`Pro`、`Max`、`MCP`、`API`、`CDN`、`Chromium`、`GitHub`、`Tampermonkey`、
`Ctrl`、`Shift`、`Alt`、`Enter`、`Esc`、`Tab`、`README`、`LICENSE` 等。

**仍然會出現、但不該翻的**：你自己的資料（帳號名、討論串標題、專案名）沒辦法用規則認出來，
盤點清單裡看到直接略過就好。長遠的解法是把專案時間軸的討論串標題加進 `protect`，
需要那一段的 DOM 結構才能做。

---

# 第六批（2026-09-21，v3.13.0，第二次 `Ctrl + Alt + E` 累積匯出）

匯出時是 289 條（腳本還停在 v3.10.0）。拿 v3.12.0 的字典重跑之後：

| 結果 | 條數 |
|---|---|
| v3.11／v3.12 已經補掉的 | 8 |
| 既有的 `looksLikeData()` 或 `never` 已擋掉、只是舊版沒擋 | 31 |
| 這次新收（字典 64 條 + 規則 14 條） | 88 |
| 這次改成自動過濾，以後不再回報 | 152 |
| 看得到但刻意不翻 | 32 |

## 這次改成自動過濾的

| 類別 | 條數 | 例子 | 為什麼 |
|---|---|---|---|
| 表情符號 shortcode | 約 130 | `:smile:`、`:point_up_2:`、`:zipper_mouth_face:` | 這是 emoji 搜尋用的識別碼，不是介面文字。翻了就搜不到。一個 emoji 面板打開一次就吐一百多條，不濾掉整份清單都是它 |
| 單獨的月份全名 | 2 | `April`、`July` | 日期照既定政策維持英文，不必每次都回報 |
| 星期全名開頭的長日期 | 1 | `Monday, September 21, 2026 at 8:46:55 PM` | 同上。舊的日期規則有 30 字上限，吃不到這麼長的 |
| 副檔名標籤（進 `never`） | 若干 | `js`、`md`、`html`、`css` | 作品清單上的檔案型別標籤，是識別碼 |

## 看得到但刻意不翻的 32 條

| 類別 | 例子 | 為什麼 |
|---|---|---|
| 你自己的資料 | `Harry`、`3 post ideas`、`Billing migration`、`Marketing analytics`、`Product review`、`Price card page verification`、`Finalize the Q3 launch plan`、`Mentioned on a podcast` | 討論串標題、任務名稱、人名，都是使用者資料。規則認不出來，只能略過 |
| Claude 執行中的即時狀態 | `Adding repository`、`Running a command`、`Reading bdb1cf72-image.png`、`Reviewing Artifacts page strings for translation gaps`、`v3.11.1 pushed; awaiting decision…` | 這些是即時生成的句子，不是固定介面字串，收進字典也不會再命中 |
| 交談內容被抓成文字 | `Fork、commit、Actions、Wi`、`outerHT`、`aria-label`、`v3.9.0 commit` | 你把前一份清單或我的回覆貼在交談裡，那些字就變成頁面內容。`Fork、commit、Actions、Wi` 還是 DOM 切斷的半句 |
| DOM 被切斷或合併的片段 | `Approaching weekly limit Get more usage`、`Thread — View thread — Idle` | 兩個以上的元素被蒐集工具串在一起。整段比對永遠不會命中這種字串，收了也是死條目 |
| 泛用狀態字 | `done`、`not done`、`running`、`in progress`、`2 still open`、`5 completed` | 太泛用，收進字典會改到你的檔名或資料夾名。這正是 v3.2.0 `docs` 資料夾被改掉的那一類風險 |

## 這次新收的重點

- **表情符號面板**：`Search emoji`、`Emoji search results`，以及八個分類標題（`Smileys & People` → 表情與人物…）。shortcode 本身不翻。
- **排程任務**一整塊：頁面框架（`Scheduled tasks`、`New task`、`No scheduled tasks yet.`）、六個官方範本名（`Daily briefing` → 每日簡報…）與六句範本說明、執行時間規則（`Every Monday at 9:00 AM` → 每週一 上午 9:00、`Weekdays at 8:00 AM` → 平日 上午 8:00）。
- **用量面板**補齊：`Usage`、`Plan usage limits`、`Context window`、`share of project`、`That is your tightest limit right now.`，以及 `Resets Sun 5:00 PM · Compacts automatically`、`Weekly · all models: 32%`、`32% of Weekly · all models used.` 這幾條規則。
- **作品頁**補齊：`Search your artifacts`、`No shared artifacts yet`、空狀態說明句、`Public` / `Document` / `Design`。
- **分頁標題**：`^(.+) - Claude$` 規則加上 `<title>` 單獨處理（見 README 第 6.9 段）。前半查得到字典才翻，你自己的交談名稱不會被動到。
- 其他：`Evening, Harry` → 晚安，Harry（逗號後面的名字原樣帶過去）、`in 45 min` → 45 分鐘後、`just now` → 剛剛、`Remove <檔名>` 只在屬性裡翻。

## 翻了但有風險、想退掉就退

`Document` → 文件、`Design` → 設計。這兩個是作品頁的型別篩選標籤，固定是介面，
留英文會讓那排半中半英。代價是你的作品剛好叫 `Document` 或 `Design` 時也會被改到。
要退掉，把字典裡那兩行的 key 搬進站台設定的 `attrOnly` 陣列就好，翻譯只會在 `aria-label` 生效。

---

# 第七批（2026-09-22，v3.14.0，`Ctrl + Alt + D` 單頁盤點）

266 條，標頭寫 **v3.10.0　字典 573 條　規則 49 條**——瀏覽器跑的還是舊版，
所以這份清單裡絕大多數在 v3.11～v3.13 早就補掉了。**拿標頭的版本／條數當版本指紋，
先確認跑的是哪一版，再判斷是不是真的漏翻。**

跟第六批逐條比對，**真正新出現的只有一條：`Drag to pin`**。其餘 265 條完全重複
（第六批是 `Ctrl + Alt + E` 跨頁累積，這批是單頁盤點，所以少了幾條不在這一頁的）。

## 這次新收

`Drag to pin` → 拖曳以釘選。字典裡本來只有 `Release to unpin`，其他三句都沒收，
拖放到一半就會中英夾雜，所以四句一起補齊：

| 原文 | 譯文 |
|---|---|
| `Drag to pin` | 拖曳以釘選 |
| `Drag to unpin` | 拖曳以取消釘選 |
| `Release to pin` | 放開以釘選 |
| `Release to unpin` | 放開以取消釘選（v3.13.0 之前就有） |

又是「成對的狀態字要一起找」那個教訓（同 v3.11.0 的 `Edited` / `Viewed`）。

## 仍然不翻的 30 條

跟第六批完全相同，理由見上一節，不重複列。

---

# 第八批（2026-09-22，v3.14.1，更新到最新版後的第一次盤點）

177 條，標頭終於是 **v3.14.0　字典 644 條　規則 71 條**。

但 132 條表情符號 shortcode 又出現了，明明 v3.13.0 就寫了過濾。查出來是
`diagnose()` 的 bug：它拿**沒正規化的原字串**去跑 `looksLikeData()`，
而頁面上的文字節點帶著前後換行與縮排，`:smile:\n` 過不了 `/^:[a-z0-9_+-]+:$/`。
`README.md`、`github.com`、`April`、`July`、長日期漏出來也是同一個原因
（「整串沒有空白」那條被換行破壞）。`Ctrl + Alt + E` 用的 `recordMiss()`
本來就是先 `normalize()` 再比對，所以兩個鍵結果一直不一致。v3.14.1 修掉。

修正後這一頁會從 **177 條降到 33 條**。

## 剩下的 33 條全是同一個問題

沒有一條是 claude.ai 的介面字串。分別是：

- **專案時間軸的討論串標題**：`Billing migration`、`Marketing analytics`、`Product review`、`Price card page verification`
- **討論串的進度清單**：`Inventorying Artifacts page localization (6 sites); v3.12.0 pending`、`Processing first collected list (34 items)…`、`2 still open`、`5 completed`、`in progress`、`done`、`not done`、`running`
- **你貼進交談的清單內容**：`README.md`、`Remove image.png`、`Usage: Weekly · all models: 31%…` 這幾條是你把上一份清單貼回來，清單本身變成頁面文字
- **被 DOM 切斷的片段**：`Fork、commit、Actions、Wi`、`outerHT`

**根因是專案介面的訊息內容不在 `protect` 裡。** 站台設定的保護區用的是交談頁的選擇器
（`[data-testid="user-message"]`、`.font-user-message`、`.font-claude-message`），
專案的時間軸與討論串顯然用別的結構，所以訊息內容、討論串標題、進度清單全都暴露在整段比對下。

這不只是清單有雜訊而已，**是安全問題**：討論串萬一剛好叫 `Settings`、`Status` 之類，
整段比對會把使用者資料改掉，跟 v3.2.0 `docs` 資料夾被改名那次同一類風險。
要修需要那幾段的實際 DOM 結構。

---

# 第九批（2026-09-22，v3.15.0，`Ctrl + Alt + E` 跨頁累積 771 條）

這批涵蓋的是以前沒逛過的畫面：**連接器目錄、外掛與技能市集、專案總覽與活動面板**。
真正的介面字串很多，新收 130 條字典 + 26 條規則。

| 分類 | 大約條數 | 處理方式 |
|---|---|---|
| 真的是介面，已收 | 約 290 | 字典或規則 |
| 連接器／外掛產品名 | 約 60 | 進 `never`，保留英文 |
| 表情符號 shortcode | 132 | 自動過濾（v3.13.0 起） |
| 連接器說明文案 | 約 90 | **不翻**，理由見下 |
| 我的工具執行說明 | 約 30 | 不翻 |
| 你自己的資料與 DOM 碎片 | 其餘 | 不翻 |

## ~~連接器說明文案為什麼不翻~~（v3.16.0 推翻，Harry 要求要翻）

原本的理由是：數量沒有上限、廠商改一個字條目就失效、不影響操作。
**Harry 看過之後要求照翻，v3.16.0 收了 65 句**（目錄上實際看過的那些）。
前兩個理由依然成立，所以這一區是**已知會慢慢長出死條目的地方**：
廠商改文案就會變回英文，逛到沒收過的連接器也一樣。這是說好的取捨，不是 bug。

## 拖放釘選的碎片沒收

清單裡有 `Drag`、`Drag to`、`to pin`、`to unpin`、`Release`、`pin` —— 這是
`Drag to pin` / `Release to unpin` 在拖曳過程被 DOM 拆成兩個文字節點。
兩端都收、語順也對得起來（`Drag` + `to pin` → 拖曳 + 以釘選），但 `pin`、`Drag`、
`Release` 這種單字收進字典的誤改風險太高，而它只是拖曳中一閃而過的提示。
整句在同一個節點時已經會翻（v3.14.0 收的），所以這裡停手。

## ~~目錄的服務條款句子沒收~~（v3.18.0 推翻）

~~`Submission to the Directory is governed by the` + `Software Directory Terms`
是一句被連結切成兩段。照 v3.6.0 定的原則「兩端都收、而且翻完語順正確時才做」——
中文得寫成「提交至目錄受《軟體目錄條款》規範」，「規範」兩個字沒地方放，
拼起來會少一截，所以不收。~~

**v3.18.0 改收了。** 當時卡住是因為我把中文想成「受……規範」這種前後包夾的句型。
改寫成「提交至目錄須遵守」＋「軟體目錄條款」，動詞全放在前半段，後半段只剩名詞，
四段拼起來語順就正確了，見第十二批。

---

# 第十批（2026-09-22，v3.16.0，補翻前一批刻意留下的兩類）

Harry 看了第九批的分類表，指名「連接器的說明文案」與「我執行工具的說明文字」這兩類要翻。

## 連接器說明文案：全收了（65 句）

見上面第九批那一節的刪除線說明。

順帶修掉一個卡了很久沒發現的上限：`looksTranslatable()` 原本只看 400 字以內…
不，原本是 **200 字**以內才查字典。最長的一句說明文案本文就有 199 字，
文字節點還會帶前後換行與縮排，剛好卡在 200 上，整句永遠翻不出來。
上限放寬到 400。同一個問題也影響到更早收的
`Claude Tag is set up from a Claude Team or Enterprise workspace…`（190 字），
等於那條字典條目一直是死的。

## 工具狀態文字：只收得到固定的那幾個

活動面板上的文字分兩種，只有第一種收得進字典：

| 種類 | 例子 | 能不能收 |
|---|---|---|
| 固定的工具狀態 | `Running a command`、`Adding repository`、`Posting a message`、`Reading <檔名>` | **可以**，v3.16.0 收了 |
| Claude 當下生成的說明 | `Building classification harness`、`Classify all 289 strings against v3.12.0`、`Diff second list against first` | **不行** |

第二種是 Claude 每呼叫一次工具就臨時寫一句，**同一句幾乎不會出現第二次**。
字典是整段完全相同才替換，收進去的那一句下次就不會再出現，等於一收進去就是死條目。
真正的解法是把活動面板加進 `protect`（那本來就是 Claude 的輸出，跟回覆同一類），
需要那一段的 DOM 結構才能做。

`Reading <檔名>` 的規則有限定副檔名，所以叫 `Reading list` 的東西不會被改掉。

---

# 第十一批（2026-09-22，v3.17.0，`Ctrl + Alt + E` 602 條）

標頭已經是 v3.16.0，但清單裡還是一堆 v3.16.0 早就翻好的東西（連接器說明文案整批都在）。
**原因是累積清單存在 `localStorage`，只有按 `Ctrl + Alt + E` 匯出才會清空**，
腳本更新不會動它。所以那些字串是他還在 v3.14／v3.15 時記下的，更新後仍留在存檔裡。

**v3.17.0 修掉了**：讀回存檔時用現在這一版的規則重篩一遍（`stillMissing()`），
已經翻得出來、已經被過濾、或列在 `never` / `attrOnly` 的直接丟掉，並把結果寫回存檔。
篩掉幾條會印在主控台。以後更新腳本之後匯出，清單會自己變短。

## 這次真正新出現的（已全收，33 條）

- **目錄的完整分類清單**（他點了「All categories」）：`AI models & agents`、
  `Customer support`、`Designs & media`、`Email & messages`、`Healthcare & life sciences`、
  `HR`、`IT & security`、`Learning`、`Logs & errors`、`Media & entertainment`、
  `Notes & memory`、`Product`、`Reference docs`、`Retail & e-commerce`、
  `Servers & cloud`、`Shopping`、`Websites`、`All categories`。
- **排序與外掛頁按鈕**：`Most installed`、`Recently added`、`Create a plugin`、
  `Upload plugin`、`Change project`、`Remove from project`、`Claude history`、`Pull requests`。
  （`Remove from project` 本來會被 `^Remove (.+)$` 規則拼成「移除 from project」，
  收進字典就正確了——字典命中優先於規則。）
- **7 句外掛的長說明**（Wingspan、Chronograph GP/LP、Postiz、Leadfeeder、incident.io、業務組合）。
  最長 293 字，靠 v3.16.0 把上限放寬到 400 才收得進來。

## 其餘 569 條

全是前幾批處理過的類別：更新前記下的舊項目、連接器產品名、我執行工具的即時說明、
你自己的資料、DOM 碎片。重篩機制上線後，下次匯出就不會再看到前兩類。

---

# 第十二批（2026-09-22，v3.18.0，外掛頁／連接器目錄／技能頁三張單頁盤點，共 87 條）

## 先講最重要的：彎撇號讓 6 個條目整整一版沒生效

三張清單裡有 6 條，字典**早就收了**卻一直被回報成漏翻：

- `Create content, plan campaigns, and analyze performance...`（行銷技能說明，v3.17.0 收）
- `Create, customize, and manage plugins tailored to your organization's tools...`（外掛技能說明）
- `Access your company's SharePoint, OneDrive, Outlook, and Teams directly in Claude`（v3.16.0 收）
- `Maryland's neighborhood development data platform.`（v3.16.0 收）
- 專案總覽的 `Claude will track your threads here as they start: what's working...`
- 設定頁的 `Recycle the worker running this project's coordinator...`

原因是**撇號有兩種寫法**：我當初從網頁複製下來時抓到的是彎撇號 `'`（U+2019），
而頁面實際送到腳本手上的是直撇號 `'`（U+0027）。整串比對是逐字比的，
一個字元不同就整條對不上，所以那幾條**從收進字典的那天起就沒有作用過**。

**修法**：`normalize()` 多做兩件事——把 `'` `'` 一律換成 `'`、把 `"` `"` 一律換成 `"`。
字典鍵和頁面文字都會走這道手續，以後兩邊用哪種引號都對得上。

這也是為什麼同樣的句子一再出現在他的清單裡：不是沒收，是收了卻永遠命中不了。

## 這次新收的（20 條字典 + 5 條規則）

- **技能頁的欄位字**：`by you`→你建立的、`from Anthropic`→來自 Anthropic、
  `From Anthropic & Partners`→來自 Anthropic 與合作夥伴、`Last edited`→最後編輯、
  `Turn off`→關閉、`Turn on`→開啟。
- **11 句技能說明**：internal-comms、brand-guidelines、canvas-design、skill-creator、
  algorithmic-art、mcp-builder、import-memory、slack-gif-creator、morning、
  web-artifacts-builder、theme-factory。和連接器說明同一類，Anthropic 改一個字就失效。
- **目錄底下的條款句**（四段全收，見上面推翻的那節）。
- **新規則**：`^Turn off/on (.+)$`、`^More actions for (.+)$`、`^Sort by (.+)$`
  （三條都走 `known()`，查不到字典就整串不動）、以及 `^, (\d+) needs? attention$`
  ——那是「N needs attention」被 DOM 切開時逗號留在後半段的版本。

## 技能代號改列 never

`algorithmic-art`、`brand-guidelines`、`docs`、`morning`、`learn`、`pdf-viewer`…
這些是技能的識別代號，不是字詞，一律保留英文。列進 `never` 有兩個好處：
不再被回報成漏翻，而且 `known()` 認得它們，所以 `View docs` 會正確組成「查看 docs」。
同批列入的還有 `Adobe for creativity`、`S&P - Deterministic Retrieval`、
`Apollo.io`、`Windsor.ai` 這幾個產品名。

## 剩下 5 條沒處理

`Harry`、`Marketing analytics`、`Price card page verification`（你自己的專案名）、
`Nevo David`、`Wingspan Engineering`（目錄上第三方開發者的名字）。
這些本來就不該翻，但**現在還是會被記進漏翻清單**，因為專案側邊欄與目錄卡片
都不在 `protect` 保護區裡。這是同一個老問題，見第八批。

---

# 第十三批（2026-09-22，v3.19.0，`Ctrl + Alt + E` 累積 224 條）

標頭是 **v3.17.0**，所以這份是更新到 v3.18.0 之前記下的。用 v3.18.0 的規則重跑一遍：

| 分類 | 條數 |
|---|---|
| v3.18.0 已經翻得出來（更新後自動消失） | 57 |
| 只在 `aria-label` 翻，畫面上本來就不動 | 8 |
| 已被過濾／列在 `never` | 19 |
| **更新後仍會被回報** | **140** |

## 那 140 條裡，約 110 條根本不是 claude.ai 的介面

是**這個專案自己的內容**：

- 討論串標題與進度清單：`Rebuilding harness module`、`Trimming main project memory under 4KB`、
  `Updating docs, commit and pushing v3.15.0`、`v3.13.0 shipped to main; 289 strings processed`…
- Claude 的回覆內容：`Hi Harry, welcome to your new project…`、
  `. I'll keep an eye on what's running and post updates…`
- 你自己的專案名：`Marketing analytics`、`Price card layout`、`Price card page verification`
- 交談內容被 DOM 切碎的片段：`Fork、commit、Actions、Wi`、`outerHT`、`main。`、`Tampermonk`

**這些一條都不該翻**，可是現在它們不在 `protect` 保護區裡，所以
(1) 一直灌進盤點清單，(2) **萬一哪個標題剛好跟字典的鍵一模一樣，就會被整段改掉**——
就像 v3.2.0 名為 `docs` 的資料夾那次。這份清單裡的 `Release`、`Product review`、
`DOM`、`HTML`、`The`、`done`、`running` 都是隨時可能撞上的短字串。目前沒撞到只是運氣。

**v3.19.0 的處理方式不是一條條加，而是加一個工具**：`Ctrl + Alt + W`（`whereMissing()`）
會把每條沒翻到的字串連同它在網頁結構裡的位置印出來，並標示有沒有受保護。
拿到位置就能寫出保護區選擇器，這件事卡了五批就是因為一直拿不到那幾段結構。

## 這次收的（很少，因為真正的介面漏翻幾乎沒了）

- `Approaching weekly limit Get more usage`——兩顆元素被當成同一段文字抓到。
  拆開的兩條字典早就有了，這次把拼接版也收進去。

## 進 `attrOnly`（畫面不碰，只在 `aria-label` 翻）

`in progress`、`not done`、`done`、`running`、`View`。
這些是討論串卡片與進度清單的狀態字，是介面沒錯，但小寫又太泛用，
畫面上直接翻風險太高（討論串剛好叫這幾個字就會被改掉）。列進 `attrOnly`
之後不再回報，螢幕閱讀器仍讀得到中文。

## 進 `never`（刻意保留英文，不再回報）

`Drag`、`Drag to`、`to pin`、`to unpin`、`pin`、`Release`——
`Drag to pin` / `Release to unpin` 被 DOM 切成兩半的碎片。整串版本 v3.14.0 就收了，
碎片拼不回正確語順（中文得寫成「拖曳以釘選」，切點不對）。

## 沒收、要說明的兩條

- `Threads do the work on their own, and you can see their statuses in` + `used in each thread`
  ——一句被切開的導覽說明。中文語順是「你可以在**某處**看到狀態」，受詞必須插在中間，
  照 v3.6.0 的原則「翻完語順正確時才收」，這個切點拼不起來。
- `Think step by step and show reasoning for complex problems. Use specific examples.`
  ——這句可能是設定頁的**範例提示文字**，也可能是你自己填進去的偏好設定。
  從匯出的清單分不出是哪一種，翻錯就等於動到你的資料，所以先不收。
  用 `Ctrl + Alt + W` 看它的位置就能確定。

---

# 第十四批（2026-09-22，v3.20.0，第一次用 `Ctrl + Alt + W` 拿到位置）

`Ctrl + Alt + W` 在專案首頁回報 9 種位置，其中 4 種是**未受保護的使用者資料**，
這一版全部列進 `protect`：

| 選擇器 | 那裡放的是 |
|---|---|
| `.cds-user-message-body` | 時間軸上的訊息內容 |
| `[data-testid="channel-created-thread-summary"]` | 討論串卡片的摘要那一行 |
| `[data-testid="root-output-attachment"]` | 卡片上的產出檔名 |
| `[data-testid="user-menu-button"]` | 左下角你自己的名字 |

**這不只是雜訊，是真的會改到資料。** jsdom 實測：把討論串摘要、產出檔名、時間軸訊息
分別取成 `Skills` 和 `Copy`（兩個都是字典裡有的鍵），v3.19.0 會把它們改成「技能」「複製」；
加上保護區之後就不動了。同一份測試也確認導覽列的 `Projects`→專案、
一般介面的 `Scheduled`→已排程 仍然翻得到，沒有翻過頭。

## 還沒關掉的一個洞：側邊欄「最近」清單

路徑是
`div[data-row] > a[data-row-main-button] > span[data-row-label] > span[data-base-ui-tooltip-trigger].dframe-fade-label`。

麻煩的是**導覽列（新的對話、專案、作品、已排程）跟「最近」的交談清單用同一種
`[data-row-label]` 結構**，直接保護 `[data-row-label]` 會連導覽列一起變回英文。
看起來只有「最近」那些多包一層 `.dframe-fade-label` 的 tooltip 容器，
但還沒拿到導覽列那邊的路徑可以對照，所以這一版先不動，
等 `zhTwWebui.where('專案')` 的結果再決定。

## 附帶確認的事

Claude 回覆裡的行內程式碼（`Ctrl + Alt + T` 這種）回報成 `[保護區]`，
表示 `BASE_PROTECTED` 的 `code` 一直是有效的。

---

# 第十五批（2026-09-22，v3.21.0，側邊欄「最近」清單）

`zhTwWebui.where('專案')` 回報導覽列的路徑，跟「最近」那些一比，差別很乾淨：

```
導覽列   … > a[data-row-main-button][data-nav-item="projects"] > span.flex > span.truncate
最近     … > a[data-row-main-button]                           > span[data-row-label] > … > span
```

1. 導覽列的按鈕帶 `data-nav-item="projects"` 這種標記，「最近」的沒有。
2. 「最近」的文字包在 `span[data-row-label]` 裡，導覽列沒有這一層。

兩個訊號指向同一個結論，所以保護區寫成**兩個條件都要成立**：

```
[data-row-main-button]:not([data-nav-item]) [data-row-label]
```

這樣就算 Claude 之後新增導覽項目（會帶 `data-nav-item`）也不會被誤保護。

jsdom 實測：把「最近」的兩筆取成 `Skills` 與 `Copy`（字典裡都有），保護後不動；
導覽列的 `Projects`→專案、`Scheduled`→已排程 與一般介面的 `Artifacts`→作品 都正常。

## 附帶：主控台貼不上去

Harry 回報 `zhTwWebui.where('專案')` 是他手打的，因為貼不上。
那是 Chrome／Edge 的自我保護，踩了兩個坑：
1. 要先按 `Ctrl + V` 試貼一次讓警告跳出來，那句話才有效；直接打會被當成 JavaScript 執行。
2. **那句話跟著瀏覽器介面語言走**。他的 Edge 是中文，警告裡寫的是
   `Type "允許貼上" below`，打英文的 `allow pasting` 一樣噴
   `SyntaxError: Unexpected identifier 'pasting'`。
**結論：照警告裡實際印出來的那串字打，不要憑印象給英文版。** 已寫進 README 疑難排解。

